目前仓库实现里面DAG中核心的结构体目前所有字段都放到了一个大的结构体中，这样在使用的时候字段太多，不知道使用什么，另外有新字段加入的时候，字段又会变得冗长和难以维护，另外，顶点逻辑和图逻辑耦合太重，加新功能总是要改核心的结构体，容易出bug。
现在需要你帮我重构这块的代码，能把顶点和图的职责分开，让Dag只管图结构，顶点管自己的逻辑。
具体来说，你需要做的是：
1. 定义个`Vertex`接口，把顶点的行为逻辑抽象到这个里面
2. 创建具体的`vertex`结构体实现这个接口
3. 重构`DAG`结构体，把顶点相关的字段和方法分离出来
4. 保证外部API完全不变，所有现有测试都能通过
